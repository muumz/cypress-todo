{
  "name": "cypress-automation-app",
  "version": "1.0.0",
  "description": "Sample Todo widget to help understand Cypress automation",
  "keywords": [
    "cypress",
    "demo"
  ],
  "main": "src/index.js",
  "dependencies": {
    "cypress": "^12.12.0",
    "react": "16.3.2",
    "react-dom": "16.3.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {},
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "cy:report": "npm run cleanup && npm run cy_dashboard && npm run merge_reports && npm run mochaswesome_report"
  }
}
